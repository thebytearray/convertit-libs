# JitPack: https://docs.jitpack.io/android/
jdk:
  - openjdk17
before_install:
  - yes | sdkmanager --licenses >/dev/null 2>&1 || true
  - sdkmanager "ndk;29.0.13113456" "cmake;3.22.1" "platforms;android-36" "build-tools;36.1.0" >/dev/null
install:
  - ./gradlew publishAllNativeLibrariesToMavenLocal --no-daemon -x test